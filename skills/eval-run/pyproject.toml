[project]
name = "eval-run"
version = "0.1.0"
description = "Score one prompt/model/adapter change against a curated data asset before promoting it. Thin CLI wrapper around 'stimulir lab eval create-run' (which detaches) plus a single non-blocking status read. Stdlib only, no direct REST/SDK auth, no polling loop."
requires-python = ">=3.10"
dependencies = []

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
    "ruff>=0.5",
]
