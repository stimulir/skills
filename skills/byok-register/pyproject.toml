[project]
name = "byok-register"
version = "0.1.0"
description = "Human-controlled Stimulir Console BYOK registration with read-only CLI discovery and verification."
requires-python = ">=3.10"
dependencies = []

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
    "ruff>=0.5",
]
