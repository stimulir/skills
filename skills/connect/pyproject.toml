[project]
name = "connect"
version = "0.1.0"
description = "Read-only Stimulir CLI install, authentication, and workspace readiness checks."
requires-python = ">=3.10"
dependencies = []

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
    "ruff>=0.5",
]
