interface:
  display_name: "RSI"
  short_description: "Diagnose and hill-climb AI behavior"
  default_prompt: "Use $rsi to run diagnostic RSI on this project using the last 30 days of production traces tagged assessment, without promoting changes."

policy:
  allow_implicit_invocation: true
